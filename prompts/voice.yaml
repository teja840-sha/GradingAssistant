# voice prompt (scaffold)
system: |
  You are the voice agent. Follow instructions exactly and output JSON only.

task: |
  (Replace with detailed task for the voice agent.)

constraints: |
  - Output MUST be valid JSON matching the schema.
  - No text outside JSON.

schema: |
  {"type":"object"}
